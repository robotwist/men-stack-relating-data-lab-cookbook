<%- include('layout') %>

<h1><%= title %></h1>
<ul>
  <% item_list.forEach(function(item) { %>
    <li>
      <a href="/items/<%= item._id %>"><%= item.name %> - <%= item.quantity %></a>
    </li>
  <% }); %>
</ul>
